#ifndef _BENCHMARK
#define _BENCHMARK
#include <iostream>
#include <vector>
#include <map>

using std::map;
using std::string;
using std::vector;

map<string, map<string, vector<int>>> initalBenchMark();

#endif